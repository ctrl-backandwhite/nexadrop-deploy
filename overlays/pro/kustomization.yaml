apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nexadrop-pro

# Producción. Este fichero NO lo escribe la integración continua: el tag se copia aquí desde
# overlays/pre por pull request, una vez validado. Reconstruir para producción rompería la garantía
# de que se despliega exactamente lo que se probó.
resources: []

images:
  - name: registry.nx036.com/nexadrop/backend
    newTag: sha-000000
  - name: registry.nx036.com/nexadrop/frontend
    newTag: sha-000000
